#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){

  int ini, fin;
  scanf("%d %d", &ini, &fin);
  int intervalo = 0;

  if (fin > ini){
      intervalo = (fin - ini)/4;
  }

return 0;
}
